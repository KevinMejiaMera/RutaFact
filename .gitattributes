# Auto detect text files and perform LF normalization
* text=auto

# Forzar LF para scripts de shell y Docker (evita problema CRLF en Windows → Linux)
*.sh text eol=lf
Dockerfile text eol=lf
docker-compose*.yml text eol=lf
